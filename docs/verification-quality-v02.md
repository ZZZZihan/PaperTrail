# 研读质量 v0.2 实际验证 · COL-19

2026-09-05。本轮 Goal 仍在进行，工程完成与产品效果尚未判为通过。
旧 [问答 v3](verification-col-18.md) 和 [简介 v4](verification-col-19.md) 成绩保持历史身份。

## 已落地与检查

- `2399ae7`：研读卡 schema、四类内容来源及独立支持/覆盖判据。
- `a4b50d8`：部分回答、覆盖持久化、旧卡主动升级及失败回退；不发布无据断言。
- `6d2bf48`：离线独立质量评分；应用自检不能代替独立评审。
- `4de0dee`：4 篇 20 题 AI 候选留出集，来源及规则冻结，未用于应用调试。
- `8fb80483c9103101ff8c0e8c4b5923cb95add10d`：真实运行前冻结原 react-03 条件注释。

上述应用核心的 `make check` 实际通过，299 项后端测试通过，包含临时 PostgreSQL 持久化测试、
Ruff、TypeScript/Vite 构建、HTTP/OpenAPI 冒烟及差异检查。
日志：`/tmp/papertrail-quality-v02-check-final.log`。
一次更早整合检查发现离线浏览器 fixture 仍用旧 query/verifier JSON，修正后已完整复跑；
此前失败日志保留，不将失败说成通过。独立集成审查未发现新的 P1/P2，69 项聚焦离线测试通过。
后续修复需以自己的最终代码 SHA 与新检查记录为准，不能继承本段成绩。

## 首次真实质量回归

两次运行的代码快照均为 `8fb80483c9103101ff8c0e8c4b5923cb95add10d`，工作区干净；
模型为真实 `gpt-5.6-sol`，经本地应用和原持久账本调用。

### 原 react-03：正确显示部分，内容仍缺两条件

运行：`data/diagnostics/runs/20260905T084326.951555Z-1606858a67d94301beb20e16b7a5e70c`。
提示版本 `evidence-qa-v4-coverage`，实际终态 `partial_answer`，3 次调用。
原问题与原答案点未改，条件注释在提交问题前冻结；预选范围只有这一题。

| 独立 AI 指标 | 实际结果 |
| --- | --- |
| 完整有据回答 | 0/1 |
| 原答案点完整覆盖 | 0/2；两项均部分覆盖 |
| 必要条件覆盖 | 0/2 |
| 已输出原子事实支持 | 6/6 |
| 检索充分性 | 0/1 |
| 误拒答、工程失败 | 分别 0/1 |
| 引用存在及归属 | 2 处重新核对通过 |
| 无据作答不足题指标 | 本次未选不足题，分母为 0 |

模型冻结条件位于物理第 3 页的主要设置段，未入选此次 12 个片段；人工编写轨迹条件已在
选中内容和实际引文中，但回答正文没有解释。前者是检索缺口，后者是生成覆盖遗漏。
应用成功识别后一缺项并显示部分回答，但不能据此宣称完整性问题已修复。
应用耗时 25.374 秒，运行器 25.666 秒，已知用量 12,117 tokens，费用未知。

[逐题独立摘要](../evals/quality-v0.2/react-03-review-2026-09-05-run1.json) 包含完整证据路径与哈希；
详细审查在独立 `data/diagnostics/reviews/` 目录，原运行 19 项 artifacts 校验均通过。
独立 AI 评审没有读取应用支持/覆盖 verdict 充当成绩，没有新增 provider 调用，人工仍 pending。

### ReAct 研读卡 v5：末次检查超时，未发布

运行：`data/diagnostics/introduction-runs/20260905T084456.761581Z-8da9f20e4eab40319afc406d826c09cd`。
提示 `paper-introduction-v5`，新任务主动升级旧卡；终态 `failed / model_timeout`。
生成、检查、一次修订、再次检查分别 30.620、33.837、26.780、88.679 秒，总计 180.014 秒。
第四次检查触及总体期限，返回用量未知；全部 4 次进入原账本，费用均未知。

首次检查的五项覆盖均通过，但方法段与结果段未给足本项支持引文，因此进入唯一一次修订。
独立 AI 读源后确认：修订稿结果段新增的冻结模型、人工分析限定仍没有附在该段的引用中，
其四个引用槽已用满；方法和边界解释在修订中也有所减弱。论文中有事实不等于本条引用足够。
这份候选没有发布，不能用独立阅读意见替代未完成的应用检查；旧 v4 成功简介仍可读。

## 来源、迁移与浏览器实际证据

本轮开始前快照：`data/diagnostics/quality-v02/baseline-20260905T083118.927058Z/before.json`。
新版启动完成数据库增量迁移后逐字段比较：3 篇论文、69 页文字、62 条旧任务、153 条旧调用均未改变，
原 PDF SHA256 全部相同，原 budget scope 未变。比较记录为同目录 `after-migration.json`。

本机浏览器实际查看了以下行为（不是仅 API 推断）：

- 旧 ReAct 简介显示尚未按研读卡规则核对的提示，以及主动补全入口。
- 新任务运行时禁用补全按钮，同时保留旧卡正文；超时后显示本次失败并保留旧卡及重试入口。
- 刷新问答历史后显示本次 `partial_answer`、已回答要点和人工轨迹待核对项。
- 点击第二处引用跳转物理 PDF 第 4 页，页码、原文引用提示及 PDF 图像一致。

完整停止并重启本地应用和 PostgreSQL 后，逐字段对比 3 篇论文、69 页、64 条任务和 160 条调用，
所有行及 PDF 哈希均完全一致，scope 与 160 上限不变。
证据：`data/diagnostics/quality-v02/restart-20260905T0855/before.json` 与 `after.json`。
重启后浏览器再次确认第 4 页、旧简介和本次超时提示、20 条 ReAct 问答历史及部分回答/缺失项均恢复。
成功新版研读卡及最终修复的真实回归仍待新增授权后执行，不将缓存可读等同于新版效果通过。

## 根据失败准备的下一次修复

修复代码 `248a50d04d7ffa00ad75eab11de366d786510f72` 已本地提交并加载：

- 问答 `evidence-qa-v5-setup-coverage` 明确将模型参数更新、few-shot 轨迹来源作为相关实验设置要点，
  留一条检索词针对条件；生成必须把条件写入正文。原 BM25/12 段/20,000 字符保持。
- 同条件离线比较支持上述最小查询改动，轮流融合和邻接补充没有解决此次缺口，RRF 还造成定位回退；
  [完整比较](retrieval-coverage-comparison.md) 保留成功与失败候选。16/16 仅是定位命中，非真实完整答案成绩。
- 简介 `paper-introduction-v6` 每项最多 8 个准确原文片段，修订优先补正确引用，保留必要流程/条件；
  五项覆盖与逐条支持门槛不降低。独立总期限 300 秒、单次至少 120 秒、过期回收 7 分钟，仍最多 4 次调用。
  QA 仍为 180 秒、每事实 4 引用、5 分钟回收；确定性测试覆盖两者边界，简介 9 引用及 QA 5 引用均拒绝。

最终代码的 `make check` 通过，**310 项后端测试通过**，含专属期限、8/9 引用边界、QA 不受影响及数据库
回收窗口测试，Ruff/格式、TypeScript/Vite、HTTP/OpenAPI 与差异检查均通过。
该检查执行于同一代码工作树，提交前后代码字节相同，其后仅两处文档措辞调整。
日志和旧失败日志已归档于 `data/diagnostics/quality-v02/checks-248a50d/`，manifest 保存 SHA256。

已请求将原累计上限从 160 增至 167，仅用于原 react-03 最多 3 次及 ReAct v6 研读卡最多 4 次。
2026-09-05 09:13 UTC 用户明确批准上述测试范围；随后核对同 scope 已用 160、无活跃任务，
仅把本机累计上限改为 167，模型和模式保持。授权记录为
`data/diagnostics/quality-v02/authorization-167/authorization.json`。后续结果单独记录，不覆盖首次失败。

## 额度与评测范围

本轮开始前同 scope 累计 153/160；上述两次运行共新增 7 次，现为 **160/160**，无活跃模型任务。
达到授权上限后不再真实调用，不换 scope、不清账本、不将未知费用算作免费。
以上为新增授权前的历史账本状态。用户随后批准最多 7 次代表性修复测试，当前上限 167；
执行完成后按实际结果记录使用量，不执行 20 题全量留出评测。

候选留出集已准备 4 篇、20 题，含 62 个答案点、29 个条件、46 个上下文；
模型运行为 **0/20**，人工核对 pending，未用来调参。这是来源与规则准备，不能宣称新论文准确率。

本地入口仍为 http://127.0.0.1:8000/，目前可读原 PDF、旧成功简介及所有历史结果。
试读步骤见 [人工试读清单](manual-trial-v02.md)。人工产品认可和学习掌握仍由用户实际反馈记录。

## 远端 CI 补充

文档交付 `681d5eeaa4415f8cd3148813019688d2b9730ba1` 的
[push CI](https://github.com/ZZZZihan/PaperTrail/actions/runs/33956961759) 与
[PR CI](https://github.com/ZZZZihan/PaperTrail/actions/runs/33956963082) 均成功。
已读取 PR CI 实际日志：310 项后端测试通过，Ruff、TypeScript/Vite、已安装包 HTTP/OpenAPI 冒烟及差异检查通过；
CI 使用 PostgreSQL 17 测试服务。这是工程检查，不代表尚未执行的最终模型效果。

## 新增授权后的真实运行与独立核对

用户授权同账本累计上限 160→167 后，以下两次运行均使用干净代码
`b785a9667a717fc99125a5e02c1eaa3209b86982`（核心 `248a50d`），仍为真实 `gpt-5.6-sol`。

### QA v5：原 react-03 两条件在本次输出中完整修复

运行 `data/diagnostics/runs/20260905T091447.930920Z-f626602b67854d9ab24958ea7edceb17`。
正文明确写出“参数冻结的 PaLM-540B”和“由人工编写 ReAct 格式的 few-shot 轨迹”；
物理第 3 页设置段本次进入检索并成为第一条实际引用，第 4 页人工轨迹条件也进入正文。

| 独立 AI 指标 | 本次结果 |
| --- | --- |
| 完整有据回答 | 1/1 |
| 原答案点完整覆盖 | 2/2 |
| 必要条件覆盖 | 2/2 |
| 去重原子事实支持 | 16/16 |
| 检索充分性 | 1/1 |
| 误拒答、工程失败 | 分别 0/1 |
| 引用存在与归属 | 3 处重新核对通过 |
| 不足题的无据作答指标 | 未选不足题，分母为 0 |

实际 3 次调用，13,449 tokens；应用 32.196 秒、运行器 32.444 秒，费用未知。
[独立摘要与绑定证据](../evals/quality-v0.2/react-03-review-2026-09-05-run2.json)
未用应用 verdict 充当评分，原 RUN 19 项 artifact 哈希全部通过。
两次使用相同冻结判据，首次失败保持原样；单题修复成功不等于全 15 题重跑或新论文泛化通过。

### 简介 v6：发布成功，但独立审查发现一处本项引用不足

运行 `data/diagnostics/introduction-runs/20260905T091613.828372Z-d81fff94c0ad4af1b1a3e4a2543c96d2`。
实际 `answered`、2 次调用、55.486 秒、97,364 tokens，费用未知。
应用返回五项覆盖通过；23 处引用及 18 个唯一片段的原文、物理页、身份复核通过。
独立判定 **7/8 输出单元完整支持，结果段 1/8 部分支持**；五项基本内容覆盖 5/5，术语 3/3。
结果段把“少样本”施加到四类任务，但本项引用仅对两个交互任务明示一/二样本，
HotpotQA/FEVER 的少样本依据只在方法与术语的引用中。因此不能用全文事实正确替代本项引用完整。

[独立 v6 摘要](../evals/quality-v0.2/intro-react-v6-review-2026-09-05.json)
保留该 false-positive：应用支持检查通过，独立检查未完整通过。原 RUN 17 项 artifact 哈希全符，
原发布结果、源文件与失败历史均未改写。最终质量完成仍需本项依据补齐后的实际验证。

### v6 发布后的浏览器与重启

实际浏览器已看到新版五项正文、3 个术语、论文陈述标签、四类来源说明与五项覆盖提示；
展开方法引用并点击冻结模型段，跳到物理 PDF 第 3 页，原文提示和 PDF 图像一致。
证据问答显示 21 条历史，新完整回答与上一条部分回答并存，两个条件均在最新正文。
起初 `cua.getState()` 遇到工具连接错误；随后通过指定本地阅读页恢复 CUA 浏览器控制，
以下行为确经浏览器执行，不以 HTTP 结果代替界面证据。

完整停止应用及 PostgreSQL 后再启动，逐字段核对 3 篇论文、69 页、66 条任务、165 条调用及 1 条请求别名，
全部行与 PDF 哈希完全一致，scope/mode 不变，累计上限 167。
新 QA 与 v6 卡的 API 完整响应与终态文件逐字段一致。
浏览器刷新后新卡、来源标签、21 条问答历史和两条件正文均恢复。
证据：`data/diagnostics/quality-v02/restart-v6-20260905T0919/`。
保存与恢复成功不改变 v6 独立质量仍为部分支持的结论。

### 后续 v7 最小修正

生成和核对均明确检查并列任务共享的每一个设置限定：结果字段同时选择设置依据与结果依据，
逐任务核对，不得用另一字段、完整论文或另一任务的条件为本项补分。
正常仍 2 次、一次内容修订后最多 4 次，原 300 秒/8 引用上限不变。
旧研读卡平时仍复用；只有主动请求且检查版本已过时才新建核对任务，原 ID 与活跃任务保持幂等。
这次修正只使用已有 ReAct 开发失败，不读取新留出答案。当前已用 165/167，余下最多 2 次用于本次修正验证。


### v7 实际运行：审核拦截引用缺口，额度不足以执行修订

干净代码 `9c6963954732cca9fe9741ffa2a1964c42e1b0e1`，运行
`data/diagnostics/introduction-runs/20260905T092951.023414Z-9f0e39ee1bd347e5b6ed7ba01fbfa8d3`。
实际终态 `failed / call_limit_exceeded`，2 次真实调用、81.613 秒、98,935 tokens，费用未知。
首次生成与审核完成，审核指出 problem 的进度/反馈调整、evidence_and_limits 的共享设置，
以及 terms[1] 的“移动”动作例子缺少各自本项引文。程序进入原定唯一修订前，账本拒绝下一调用。
第二 attempt 只记录了待修订阶段，没有生成稿，不能算成第三次实际模型调用。

原候选、反馈与失败均保留，新 `introduction=null`；此前 v6 卡继续作为旧核对版本可读。
识别缺口证明此次审核发挥了拦截作用，但不能据此声称卡的完整语义目标已完成。
此后不再调用模型，scope、模式、模型及累计上限均不改变。

[独立 v7 候选审查](../evals/quality-v0.2/intro-react-v7-review-2026-09-05.json) 另判未发布候选 6/8 单元本项引用完整支持、2/8 部分；
22 处引用身份及原 RUN 17 项哈希通过。结果段设置/行为局限与术语“移动”例子需补本项证据，
应用与独立审查对问题段概括的判断不同，详细报告保留该分歧，不读取应用 verdict 代填独立结论。


## 最终工程修复与当前交接

代码 `be9c664660835122816449cebc8e97c44f99a22d` 修复主动更新入口的事务边界：
简介展示元数据在原创建事务内完成，原 ID、新 alias、新任务三种返回都没有提交后的二次连接失败窗口。
4 个真实 SQL 故障回归确认返回 503 时任务/alias 一并回滚，没有未启动的孤立 pending，恢复后同 ID 可正常调度一次。
独立复查确认原 P2 已解决，未发现新的 P1/P2。

最终 `make check` **退出码 0、321 项后端测试通过**，含临时 PostgreSQL、Ruff/格式、
TypeScript/Vite、已安装包 HTTP/OpenAPI/docs 及差异检查。
证据 `data/diagnostics/quality-v02/checks-be9c664/`，日志 SHA256
`c8ec059634791a09801003f2d6a9c06bdea3877d6e1088199f75f05122c262b1`。
前一代码 `9c696395` 的 317 项检查完整日志保留；该次 shell 外层误写 zsh 只读变量导致包装命令退出 1，
没有隐藏此错误。随后相同核心的 [PR CI](https://github.com/ZZZZihan/PaperTrail/actions/runs/33958176366)
及 [push CI](https://github.com/ZZZZihan/PaperTrail/actions/runs/33958174055) 均成功。
本段最终 321 项检查用独立捕获退出码的执行方式，实际退出码为 0。

最终代码加载并完整重启应用和 PostgreSQL 后，3 篇论文、69 页、67 条任务、167 条调用及 1 条请求别名
逐字段完全一致，PDF 哈希一致，scope/mode/167 上限不变。最新 v7 失败 API 响应与保存的终态完全相同，
v6 旧卡继续可读且标记核对版本过时；原 react-03 两条件正文与 21 条历史保留。
证据 `data/diagnostics/quality-v02/restart-final-20260905T0937/`。

**当前同账本 167/167、无活跃模型任务。** 新增获准 7 次全部使用：QA 3 次、v6 卡 2 次、v7 卡 2 次。
尚未执行成功的完整 v7 生成/修订/再核对，Goal 不记完成。已提出新增最多 8 次（上限 175）的明确请求，
只用于最多两轮 ReAct 卡完整验证，成功即停；尚未批准前不改上限或再调用。
新 4 篇 20 题候选留出集仍为模型 0/20、未参与调参，人工、产品认可与学习验收仍 pending。

最终文档检查：132 个本地 Markdown 链接有效；JSON 独立摘要可解析。远端交付以最后推送 SHA 和 PR checks 为准。


## 2026-09-05 临时不限量授权后续验证

用户明确授权“我批准你不限量去使用。暂时批准你不限量去使用。”，解除此前 167 次上限的阻塞。
授权前实时账本为同一 scope 的 provider_quota 167/167、无活跃任务，模型 gpt-5.6-sol；
3 篇论文、69 页、67 条任务和历史失败保留。授权记录：
`data/diagnostics/quality-v02/authorization-unlimited-20260905T115929-34e69835/authorization.json`。

本次只为当前 Goal 的必要实现与测试启用显式 `PAPERTRAIL_MODEL_MAX_CALLS=unlimited`，
不更换 scope、模式或模型，不清空账本；未知费率与费用仍为未知。
每任务 3 次问答 / 最多 4 次简介调用、总期限和唯一一次简介内容修订保持。
临时不限量在本 Goal 工程验证完成或用户撤销时关闭，数值上限设为届时实际累计次数。

后续验证顺序：新版 ReAct 完整卡及独立核对；旧 3 篇 15 题回归；
在应用代码固定后，独立执行新 4 篇 20 题候选留出集并报告原始分母与失败。
原文/题目/规则及历史失败不修改，不将留出答案反馈给实现调参。
这些是执行安排，实际运行与成绩将在下文依据新的不可覆盖 RUN 记录。


### 新配置检查与第一轮完整运行

代码 `ca9fabc3c84009d1ae91968d69957e551006fb17` 的 `make check` 实际退出 0：
335 项后端测试、Ruff/格式、TypeScript/Vite、安装包与 health/OpenAPI/docs smoke 通过。
只读预算审查未发现 P1/P2；显式 unlimited 保留同 scope 账本、并发预留锁和每任务限制。
日志 `data/diagnostics/quality-v02/checks-ca9fabc3/`，SHA-256
`f9891b7955ce78e5a8e9a0c2c7d4d489ca5dd9fdb06bcc55c793239093874ece`。
[该 SHA 的 PR CI](https://github.com/ZZZZihan/PaperTrail/actions/runs/33965146930) 与
[push CI](https://github.com/ZZZZihan/PaperTrail/actions/runs/33965144737) 均成功。

ReAct v7 新 RUN `20260905T120604.854158Z-52344d5fd6874774a3842848e681d064` 完成全部 4 次调用，
168.811 秒、203,547 tokens，费用未知，应用终态 `insufficient_evidence`，未发布新卡。
最终稿已补齐此前结果段的冻结/人工设置引用并说明稀疏异步思考，不能把初稿错误当作修订后的错误。
但最终核对仍发现方法输出缺该项直接来源、术语枚举缺自身来源。独立逐稿核对纠正了初步诊断：
这两项初稿已存在，首次核对判通过，末次才判不支持；不是修订新增实质错误。
因此不引入“锁定通过字段”结构来掩盖首审漏检。v8 将以通用生成和子断言核对提示修正，
要求输入/过程/输出各自有据、避免外围枚举，并逐个检查必要例子与条件；原结构和评分门槛保持。

此前并行启动的旧集 RUN `20260905T120747.657918Z-af3af42615a8444d91ea0a05a8d8db76`
15 个提交全部 HTTP 409，runner 工程失败 15/15，记录调用小计为 0、应用状态未知；
该 RUN 同时标记 15 题用量未测，不能仅从它证明实际调用为零。同期同 scope 账本观察属于独立证据。
原因是简介仍活跃时应用拒绝第二个生成任务，这是本次评测调度错误；保留所有失败，不能记成模型答错或丢弃分母。
简介结束并确认活跃任务为 0 后，再以新 RUN `20260905T121135.392911Z-9c80294018c84514ae88099cc1362811`
顺序执行同一 15 题；该 RUN 初始源码干净、与原 ca9fabc 应用一致。
旧集条件附录 `development-v01-conditions-v1.json` 已在运行前冻结，10 可答题共 11 条条件，
原 react-03 两条件逐字不变；附录 SHA-256 `a47beb1c104041b5377b21409a224864643d4fe7c597b851f252b6fe1f19cbfa`。

评测运行器与网页共用单实例的生成互斥；启动下一批真实评测前，应确认上一任务已经终态，
不要把观察超时当作停止或并行重跑。普通浏览器查看、刷新及引用核对不消耗模型调用。


### QA v5 完整旧集与简介 v8 的实际结果

旧集 RUN `20260905T121135.392911Z-9c80294018c84514ae88099cc1362811` 已全部终态：
7 answered、5 partial_answer、3 insufficient_evidence，本 RUN 工程失败 0/15。
独立 AI 评分（不采用应用自检成绩）：完整有据交付 7/10，原要点 16/18，必要条件 10/11，
原子事实支持 79/80，可答检索充分性 10/10，误拒答 0/10，不足题无据实质结论 1/5；
26 处实际引用存在、页码与归属全部通过，不能由此替代语义结论。
45 次真实调用，196,181 tokens（输入 180,060、输出 16,121），runner 逐题耗时合计 501.218 秒，费用全部未知。
逐题和独立产物哈希见 [本轮旧集摘要](../evals/quality-v0.2/development-review-2026-09-05-run121135.json)。

真实漏答为 Reflexion 的记忆保存内容、Toolformer 的一般保留阈值，所需内容均已进入检索；
react-04 正文完整却被可选背景判 partial；reflexion-05 的“其余实验”穷尽表达遗漏附录实验，
toolformer-04 未编造文档 ID/种子，但只给过滤背景，未明确所求具体信息证据不足。
因此下一步是通用的必要性判定、独立要点表达、精确信息不足与范围量词修正，检索架构保持。
不按题号绕过检查，原题、冻结条件与本次失败均不覆盖。

简介核心 `8085ef7343f80d355c17631d82023410ffe793fe`，实测源码 `1a6e23df072e252d411ddb23e614f4d585d459e4`。
`make check` 退出 0、335 项后端测试通过，其余静态/网页/安装包 smoke 同样通过；
日志 `data/diagnostics/quality-v02/checks-1a6e23df/`，SHA-256
`a14ae667ca9442fa9aa210cdbc5890b9b2865268897028882d2e56ffed080544`。
v8 RUN `20260905T122051.689720Z-9511886873e148db9255d3aac59f7f1d` 顺序执行 ReAct、Reflexion、Toolformer：
三篇均完成 4 次调用后 insufficient_evidence，未发布新卡；12 次调用、440,004 tokens、管线耗时合计 482.790 秒，费用未知。
三篇终态持久读取、相同请求重读、与问答历史分离检查通过；未发布结果的程序引用检查为 not_applicable，不能记作通过。
逐稿独立内容审查进行中，不能以应用拦截推断内容全部有错或全部正确。

以上两批结束后只读实时核对：同一 provider_quota 账本累计 228 次、0 活跃任务、显式 unlimited；
这是当时快照，后续实际运行继续累计。原 167 次授权之前全部历史保留。


### QA v6 全旧集与简介 v9 的独立失败边界

QA v6（核心 `3f69401`，运行源码 `11adc8a`）RUN
`20260905T123700.665098Z-789421e2076a49358cb7757801594e17` 完成全部 15 题：
应用 8 answered、4 partial_answer、3 insufficient_evidence；独立完整有据交付 6/10，
原点 13/18、条件 9/11、事实 82/82、可答检索充分 9/10、误拒答 0/10、
不足题无据实质结论 0/5，工程失败 0/15，实际引用 24/24 存在与归属通过。
不能把 82/82 或没有编造费用，写成回答完整或所有不足题应对恰当；react-05 仍以背景代替费用不足说明。
45 calls、196,693 tokens（输入 185,502、输出 11,191）、runner 逐题合计 343.189 秒、费用未知。
[独立逐题摘要](../evals/quality-v0.2/development-review-2026-09-05-run123700.json) 保留所有遗漏。
`make check` 335 passed / 退出 0 的日志 `data/diagnostics/quality-v02/checks-11adc8a/`，SHA-256
`ee0c519da53a30c5aa862f278f8c477deddcac8334974ae720d07f49dda9bdd4`。

此次 react-03 的模型查询包含 frozen，但冻结段在合并 BM25 中排第 15，被前 12 段裁掉；
该条件单独查询也只排第 6，因此简单的每查询预留或轮流取段没有解决。
依据固定旧运行作同条件离线比较，下一步采用有界词覆盖补充：每查询仅考虑其前 12 个候选，
总计最多补换两条尾项，保留 12 段/20,000 字符、正分、去重及原已覆盖查询词。
词覆盖不是语义充分性保证。先冻结的脚本与全部变体结果见
[比较报告](../evals/quality-v0.2/retrieval-comparison-2026-09-05.json)。
两次完整旧集及单题历史的 31 个问答实例均重放一致，原实际引用片段均保留；
v6 的 16 个证据锚点由 15/16 到 16/16，未参与新论文留出调试。

简介 v9 核心 `a7914c7`，实测源码 `a839896`，RUN
`20260905T124405.178139Z-7c29dee713ed4d6391866997512da523`：ReAct 不足、Reflexion answered、Toolformer 不足。
新 Reflexion 已持久保存并有 26 处引用存在/归属通过，但独立只 5/8 自身引文完整支持、8/8 来源类别、5/5 内容覆盖，
仍缺 Actor 输入等本项来源；不能把应用发布记成完整支持样例。
ReAct 独立 7/8 自身支持、8/8 来源类别、4/5 正确覆盖；少样本术语把环境观察放入模型模仿的施事范围，
方法的笼统任务结果表述则保留应用与独立判定分歧。Toolformer 独立 6/8 自身支持、8/8 来源类别、4/5 覆盖，
修订改动未失败的 API 术语，引入调用与返回结果的定义混淆；实验主观察仍过于笼统。
三篇原稿、修订及初次盲判后复核改判全部保留。

v9 的 `make check` 335 passed / 退出 0，日志 `data/diagnostics/quality-v02/checks-a7914c7/`，SHA-256
`4394efa22fe1ae303c19ffdbb5780b289a94cc89da09abe048211e3c20acb467`。
上述两批后同账本累计 285 次，无活跃模型任务，费用未知。
这是一份明确失败诊断，当前仍缺完整独立支持的新发布卡，Goal 工程完成继续 pending。
