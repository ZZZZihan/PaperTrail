# 必要条件检索的离线开发比较

2026-09-05 · [COL-19](https://linear.app/colife/issue/COL-19) · 配合 [研读质量 v0.2](reading-quality-v02.md)。

**决定：保留合并查询与 BM25，先完善通用查询契约。** 实验设置问题的查询应包含参数是否更新、示例怎样形成等必要条件，避免三条查询反复堆叠论文标题和任务名。本次没有修改应用检索算法，也没有使用修改后的模型查询运行新评测。

## 失败依据与比较范围

真实 `react-03` 运行 `20260905T084326.951555Z-1606858a67d94301beb20e16b7a5e70c` 已返回部分回答。人工轨迹条件在实际检索片段和引用中，但正文遗漏；冻结模型条件所在的现有块是物理第 3 页 chars `3597–4993`，原合并 BM25 排名 20，未入选。前一项需要生成正文覆盖修复，不能单靠检索解决。

离线使用原 3 篇、15 题开发集：A 组采用完整 v3 的既有查询；B 组仅将 `react-03` 替换为上述真实运行的查询，其他 14 题不变。分块、BM25 参数和上限保持一致：最多 12 段、20,000 字符。源码为 `page-char-v1-1400-200` / `bm25-v1-k1-1.5-b-0.75`。重建时逐页文字哈希与冻结 manifest 一致，全部基线选中块 ID 与原运行 trace 一致。

## 实际结果与取舍

下表分母 16 是开发集的短原文定位片段；5 道不足题没有预期定位，不能据此评判拒答质量。

| 离线候选 | A 组定位命中 | B 组定位命中 |
| --- | ---: | ---: |
| 原合并查询 BM25 | 15/16 | 15/16 |
| 原子查询轮流取块，按 ID 去重 | 15/16 | 15/16 |
| 原子查询 RRF 三路融合 | 13/16 | 13/16 |
| RRF 加原中文问题第四路 | 12/16 | 12/16 |
| 原前 8 段，再补同页或相邻块 | 15/16 | 15/16 |
| 合并 BM25，第三条换成必要条件诊断词 | 16/16 | 16/16 |

最后一行仅对原开发集 `category=experiment` 的题目保留前两条查询，将第三条替换为以下 **AI 人工设计的诊断词**；其余问题保持原查询：

```text
frozen trained parameters manual automatic demonstration provenance
```

这个离线分类选择并不是应用里新增的分类器。诊断词根据已知开发失败设计，已经参与调试；应用采用通用提示要求模型根据当前问题生成条件查询，不硬编码该论文的答案或此词串。

B 组 `react-03` 从 12 段、15,589 字符变为 12 段、16,285 字符；人工轨迹与 6/3 示例数的块排 11，冻结条件块排 12，两个定位均保留。其他 14 题的预期定位没有回退。完整逐题查询、选中块原文、ID、页码、范围、分数及命中结果保存在下方本机证据中。

保留的反例：直接在原三条查询后追加同一诊断词时，冻结块仍排 13，超出上限；另一组包含更多泛化词的条件查询虽召回冻结块，却挤出示例数，B 组仍为 15/16。RRF 三路丢失 Toolformer-02 的两个定位，四路还丢失 ReAct-01。独立检查的 15 个邻接/同页候选均未找到冻结块，部分还丢失已有示例数证据；这些失败与纠正记录一并保留，没有只留下成功候选。

**16/16 只表示这些短定位处被选中，不表示语义支持完整、回答完整或泛化准确率。** 冻结块排在 12 的边界也表明结果对查询措辞敏感。新 v5 查询提示和生成提示是否能在真实模型运行中形成完整有据答案，必须另行验证；本比较新增模型调用为 0，人工核对仍 pending，未读取或修改留出集。

## 本机证据与复现

独立证据目录为 `data/diagnostics/quality-v02/retrieval-comparison-20260905T085311Z-61a8/`，处于 Git 忽略目录，文件创建后设为 `0444`，未上传包含论文正文的比较产物。入口：[完整比较结果](../data/diagnostics/quality-v02/retrieval-comparison-20260905T085311Z-61a8/comparison.json)、[重建脚本](../data/diagnostics/quality-v02/retrieval-comparison-20260905T085311Z-61a8/replay.py)、[来源记录](../data/diagnostics/quality-v02/retrieval-comparison-20260905T085311Z-61a8/provenance.json)、[早期反例与独立邻接检查](../data/diagnostics/quality-v02/retrieval-comparison-20260905T085311Z-61a8/earlier-observations.json)。这些链接需要本机证据目录，GitHub 仅保存本摘要和哈希。

原比较通过 shell stdin 执行，未留下原始临时脚本。归档脚本据已执行代码重建，并获准进行一次相同输入、相同候选的确定性重建，以保存完整选中块；不是原封不动复制的旧脚本。重建未扩展候选，全部聚合结果与此前实际输出一致。退出码为 0，stdout 与当时的检索源码均保存。

| 产物 | SHA-256 |
| --- | --- |
| `comparison.json` | `3abd3b84a52501efe8212a62dbd98bdf993b46533336cf3c287c19256e4fae0b` |
| `replay.py` | `a41c99a87a5bd593c399c2bab3c27f9802ddd78345dc3eb117f1831de6f080f9` |
| `provenance.json` | `0047db686c83fecf596052e563b9ba536c9a6b60b3caeaf100f8fca6148fdc2e` |
| `artifacts.sha256` | `95a76b4342b48a542bdca177f76524d5667ad78f3acec644deb90b938aeb5f42` |

在仓库根目录检查证据；重建需指定一个尚不存在的新输出文件，脚本拒绝覆盖：

```bash
uv run python data/diagnostics/quality-v02/retrieval-comparison-20260905T085311Z-61a8/replay.py \
  --output /tmp/papertrail-retrieval-replay-new.json
```

该命令只读取列明的旧开发运行与本地检索代码，不调用模型、应用数据库或网络，不读取留出题。源输入与代码的逐文件哈希、实际执行命令见 `provenance.json` 和 `comparison.json`。
