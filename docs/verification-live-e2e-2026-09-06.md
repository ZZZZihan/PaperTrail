# COL-19 真实模型与浏览器端到端验证

日期：2026-09-06，北京时间。实际业务代码：`8e2bb06abb0d3d6789792c3e7f9e5d0d59641db3`。本轮没有修改业务代码、检索或模型提示；以下是测试结果，不代表质量全部通过。

## 授权与用量

用户先要求测试模型能否调用，再要求“去测试端到端”。前一连通性测试通过实际问答 API 新增 3 次调用，原 scope 从 394 到 397。本轮测试前冻结两条 ReAct 问答和一张 Reflexion 研读卡的输入与判据，最多新增 10 次调用；仅将累计上限由 397 改为 407，保持 `gpt-5.6-sol`、`provider_quota`、原 scope 和全部历史。

本轮实际使用 10 次，全部成功且供应商返回模型名与请求名一致；共 140,680 tokens，费用未知。结束时同 scope 为 **407/407**，没有活跃任务或未完成调用预留。没有启用 unlimited，也没有为测试重置账本。QA 使用 none，简介使用 low；每任务原有三次/四次调用及 180/300 秒期限保持。

## 真实模型结果与独立原文核对

所有新任务均从 Chromium 中的真实网页按钮提交，经本机应用、供应商模型、PostgreSQL 再回到网页。独立 AI 核对直接读取 SHA-256 匹配的本地 PDF，不以应用模型的自检结论替代判断。

| 场景 | 实际结果 | 调用 / 总用时 | 独立结论 |
| --- | --- | --- | --- |
| ReAct：HotpotQA/FEVER 中三种 Wikipedia 交互动作及各自作用 | `answered` | 3 / 20.060 秒 | 完整有据；3/3 claim、3/3 引用通过。search 的双分支、lookup 当前页下一句和 finish 提交答案均完整 |
| ReAct：是否报告 GPT-4 训练 GPU 型号、数量和总电费，要求具体数值 | `partial_answer` | 3 / 34.796 秒 | **预设不足行为未通过**。2/2 背景 claim 有据、3/3 引用存在，没有编造硬件或电费，但没有直接说明三个所求项目无法从当前证据获得 |
| Reflexion：从 v9 旧卡主动更新为 v10 研读卡 | `answered`，包含唯一一次修订 | 4 / 165.459 秒 | 8/8 本项支持、8/8 来源类别、5/5 必要内容覆盖，30/30 引用存在 |

任务 ID：

- 正常问答：`f0cfb81e-bbe1-4ff5-9bd5-f4293994ee0a`。
- 不足题：`4695a98f-0b12-4959-89b8-1092f2622202`。
- Reflexion 新卡：`5c98f471-330f-4d57-9a1d-55a06a698127`，旧卡 `ac23fefd-6c1f-43a9-bc75-1cf831c7c5f8` 保留。

不足题的具体缺口：正文给出了 PaLM/GPT-3、冻结模型与微调背景；覆盖检查将模型身份、冻结与否列为问题已回答项，而真正请求的 GPU 型号、数量和电费均未覆盖。事实支持通过不能替代直接回应与不足状态通过；本例恰当不足为 **0/1**。后续修复应保留这条失败，不放宽预设判据。

Reflexion 新卡的中心证据保留 ALFWorld 的 134 个环境、GPT-3、ReAct、两条少样本轨迹、启发式触发、最近三条经验及连续 12 轮内完成 130/134，不写成单次成功率；WebShop 的 100 个环境、two-shot 和四轮无改善用于限定结论。这是本张卡的独立开发核对，不能泛化为所有论文的成功率。

## 浏览器实际操作

| 路径 | 观察结果 |
| --- | --- |
| 提交、处理中刷新与返回论文 | 两条问答均由输入框提交；处理中刷新后后台完成原任务，历史恢复，未产生重复问答 |
| 跨论文并发保护 | 不足题尚在运行时，Reflexion 更新请求返回 409；未新建简介任务或新增模型调用。问答结束后再次点击返回 202，开始同一张旧卡的更新 |
| 简介更新与旧卡 | 更新期间刷新、离开论文再回来，旧卡仍可阅读；最终新卡显示已保存，版本更新提示消失 |
| 新回答引用 | 正常回答 3 个引用逐一跳到 PDF 第 4 页并高亮；不足题的三个引用跳到第 10、3、5 页并出现高亮 |
| 新研读卡引用 | 8 项共 30 个引用逐一点击，PDF 页码框与引用页一致，每个引用均有提取文字高亮；PDF canvas 可见 |
| 390×844 手机视口 | 简介底部 CTA 聚焦并显示输入框；普通标签切换保留未提交草稿。重启后第 14 页 PDF 与引文高亮仍可见，页面无横向溢出 |
| 缓存 | 来回切换、刷新卡片与浏览器重载后内容保持；受监听的缓存/引用操作 POST 数为 0，最终账本仍为 407 |
| 上传 | 通过文件选择器重传真实 ReAct PDF，返回 200 并复用原论文 ID，仍为 7 篇；无效 PDF 返回 422，网页明确提示“请上传有效的 PDF 文件。” |

自动化操作中，CLI 的文件选择模态状态需要用 upload 命令完成，因此真实 ReAct 文件重复提交两次，两次都返回同一论文。手机视口有两层“PDF 原文”tab，首次选择器产生严格匹配错误，改为限定“原文显示方式”tablist 后通过。早期 run-code 语法探测错误、这两项驱动问题与应用业务失败分开记录，不宣称所有自动化命令首次即通过。浏览器记录的 409、422 网络错误对应上表的实际并发拒绝和无效文件测试。

## 持久化与隔离回归

对真实应用和项目 PostgreSQL 17 执行停止再启动：应用 PID 从 39781 到 44222，数据库 PID 从 45192 到 44210。只读 REPEATABLE READ 事务快照比较六张表完整字段的稳定排序哈希，并比较所有 PDF 字节哈希；重启前后全部相同：**7 篇论文、168 页、145 条任务、407 条调用、1 条 alias，0 活跃任务**。重启后浏览器再次读取新卡并完成手机引用检查。

每个新模型调用的 stage、status、请求/返回模型名、usage、response SHA-256 和 completed_at 已与数据库账本逐条核对；三项任务 trace 与实际账本完全一致。

独立只读复核排除本轮三个新任务 ID 及其调用后，原 142 条任务和 397 条调用的完整行哈希均精确回到测试前基线；旧历史逐行未改。papers、pages、alias、schema 及全部 PDF 同样与测试起点一致。新增 token 明细为 130,079 输入加 10,601 输出。

另行执行 `make check-e2e`，前端构建与隔离 OFFLINE API E2E **15/15 通过、退出 0**。覆盖解析与空页、无效输入、六种问答终态、幂等、归属、PDF 字节/Range，以及隔离应用和数据库重启；临时资源已清理。这部分新增真实调用为 0，不并入上面的真实模型结果。业务代码未改，本轮没有重复运行先前已通过的全部 383 项后端测试。

## 本机原始证据与剩余工作

原始证据保存于 Git 忽略目录 `output/playwright/live-e2e-20260906/`：`plan.json`、两份 QA 结果、`introduction-result.json`、`model-and-ledger-results.json`、重启前后快照、`restart.json`、`restart-comparison.json`、浏览器快照及 PNG。独立隔离回归证据为 `output/e2e/offline-regression-20260905T235206Z.json`。

应用已恢复在 `http://127.0.0.1:8000/`。可查看 [ReAct 问答](http://127.0.0.1:8000/#paper=acffff0b-0513-4b00-98d3-40567542eda9&page=0) 和 [Reflexion 新卡](http://127.0.0.1:8000/#paper=c48760cc-266c-4d27-b677-592f2a743bbe&page=0)。下一项质量工作是区分与问题相关的背景事实和真正回答了请求的内容，使不可答核心问题明确落到证据不足。此次测试没有修改该行为，旧开发集/留出集成绩也未改写。

COL-19 实时状态仍 In Review，属于 P2，PR #2 仍 draft、未合并；人工产品认可与学习验收保持 pending。
