# 单篇论文研读质量 v0.2 · COL-19

本轮由用户的准确性与专业度 Goal 授权，接续已交付的 COL-19 demo。
旧简介核心 `13e33492baf45a13ecb61797a0cc2b1676292e6b` 与交付记录保留；
旧问答的 3 篇 15 题、react-03 部分覆盖及所有失败记录不改写。

## 用户行为与完成判据

1. 阅读简介时能理解研究问题、术语在本文的含义、方法输入/过程/输出、实验依据与必要条件、结论及范围。
   论文陈述、作者解释、教学示意和系统推断采用不同标签，后两类也不能添加无依据的论文事实。
   关键内容均能回到原文，成功结果持久保存。旧缓存不伪装成已通过新规则。
2. 提问后分别看到有依据的回答与要点覆盖情况；事实有据但条件或子问题遗漏时，显示部分回答与待核对项。
   通过支持检查的部分内容保留；无支持的断言只留在诊断追踪，不进入答案。
3. 当前引用原文存在、身份与归属校验保持；覆盖检查是 AI 判断，不等于人工评分。
   “当前已检索证据不足”不等于论文绝对没有答案，纠正有直接反证的前提可以是完整回答。
4. 页面、历史、刷新与必要重启保留新数据及旧论文/问答/账本。完成后给出试用入口、实际验证范围与未完成项。

## 固定问答流程

模型整理检索词和 1—12 个中性问题要点 → 既有 BM25 检索 → 模型按要点生成引用事实 →
程序引用检查 → 一次独立模型调用分别判断事实支持与要点覆盖。

要点整理只能看到用户问题，没有评测题号或预期答案。生成阶段必须把相关必要条件写在正文，
不能依赖引用里附带的细节。覆盖检查读取问题、要点、回答与本次检索上下文，
逐项核对，并可补充 0—8 个上下文显示的必要限定；事实支持仍只能依据该条事实所附原文。

`coverage` 是持久 JSON：`status = complete / partial / unanswered`、
`review_source = ai / not_checked`、`items`（要点、中性来源、是否覆盖、关联的已发布事实索引、诊断理由）。
支持不通过的事实被移除后，所有关联覆盖项不得继续记为覆盖，索引重算到实际发布事实。
缺失、重复、越界或非布尔判断导致检查失败，不能假定完整。

终态：`answered`（存在支持事实且全部已识别要点覆盖）、`partial_answer`（存在支持事实但有缺项）、
`insufficient_evidence`（本次没有可发布事实）、`failed`（配置、格式、校验或运行失败）。
旧任务的 coverage 为 null，界面明确旧回答尚未按新规则检查；不回填推测结果。

仍为固定最多 3 次模型调用、180 秒总期限，无自动重试。当前没有引入新检索模型、向量数据库或 Agent。
本次首先解决已找到条件但正文遗漏的问题；检索充分性由独立诊断评价，不能用短语命中代替。

## 简介、缓存与解释边界

新版简介契约和五项覆盖判据见 [简介规格](paper-introduction-demo.md)。
覆盖与支持检查共用既有 verify 调用，正常 2 次、唯一一次内容修订后最多 4 次。
首次 v5 实测最后检查触及 180 秒，且复合段落的四个引用槽不足以补齐全部限定来源。
后续 v6 为简介单独设 300 秒总期限、至少 120 秒单次窗口、每项最多 8 个原文片段；
这是增加充分核对的容量，逐项支持与五项覆盖门槛保持不变。问答仍为 180 秒、每事实最多 4 引用。
简介过期回收窗口相应为 7 分钟，问答仍为 5 分钟，避免轮询提前回收仍在合法执行的任务。
旧成功结果继续可读；只有用户主动升级旧格式或更新过时检查版本时创建新任务，其余成功结果仍幂等复用。
原请求 ID 重试始终返回原任务；升级须使用新的请求 ID。旧结果仍保存在数据库。

## 预先固定的诊断安排

- 旧 `evals/development-v0.1` 继续回归；重点重新执行 react-03 的原始问题、原始预期与原始论文。
  不能将预期注入运行提示；旧结果永远保持部分覆盖。
- 准备 4 篇新论文、20 题 AI 候选留出集，按论文隔离、来源/要点/上下文与评分规则冻结。
  覆盖直接事实、跨段条件、错误前提、真正不足及表格脚注探测。来源准备不等于模型通过。
  用于调参就失去留出资格，需如实记录并补建；人审 pending 不改成通过。
- 独立评分按已发布原子事实、必要答案要点和冻结条件判断；单列检索充分性、完整有据回答、
  误拒答、无据回答、程序引用合规、工程失败、耗时及调用数。旧集新增条件标注使用单独冻结 rubric，
  不覆盖原题库。无人工或独立 AI 评分时留空，不读取应用自检充当独立成绩。

以下是首次安排的历史快照；最终20题已执行并独立评分，见文末与验证记录。
本轮开始前实时账本153/160、无活跃任务。预留3次给真实react-03、最多4次给新版ReAct简介，
沿原scope持久累计。4篇20题全量模型评测尚未执行，按实际余量决定可执行范围；
不能将未知费用写成免费，也不能为评测重置账本。

## 首次实测后的最小修复

原 react-03 首次 v4-coverage 实测仍遗漏两项条件，应用正确显示部分回答；
冻结模型段未检索，人工轨迹在引文中但未进入正文。完整独立评分与引用证据见
[质量验证](verification-quality-v02.md)，旧预期与本次失败均保留。

v5-setup-coverage 查询契约将提示实验的模型身份与参数更新状态、few-shot 数量与轨迹来源
分别列为中性要点；至多三条检索词中留一条针对条件，避免标题/任务名反复挤占条件词。
生成契约要求这些限定进入正文。本轮继续使用原 BM25、12 段、20,000 字符，
没有根据单个失败直接更换检索算法；[同条件离线比较](retrieval-coverage-comparison.md)
只提供旧开发集定位证据，不能当作新查询模型或最终语义效果已通过。

首次两次真实运行后原累计额度达到160/160；后续修复先完成离线验证，新增真实运行须新增授权。
4篇20题候选集已冻结且模型0/20，人工核对pending，未参与上述应用调试。

## 验证计划与人工试读

代码及基础改动执行 make check；覆盖正常、部分、无据、检查格式错误、旧缓存主动升级、
幂等重读及重启。实际模型与浏览器证据另建不覆盖的运行目录，记录代码及提示版本、原文哈希、
逐题结果、引用程序检查与独立语义评判。人工认可和学习掌握独立待用户实际试读。

人工试读任务：说明论文研究问题、方法输入输出与一个关键实验条件；点击原文核对一个结论；
记录仍难理解的术语、遗漏的条件及找证据所需时间。AI 不替用户填写认可或学习结果。

2026-09-05 后续用户明确批准同 scope 160→167，最多 7 次代表性测试。
QA v5 原 react-03 独立 1/1 完整、两条件 2/2；v6 卡应用发布而独立结果段部分支持，
因此继续 v7 的并列任务条件与本项引用核对修正，旧结果与评分不覆盖。最新用量与结果见验证记录。


## 临时不限量授权与后续开发诊断

2026-09-05 用户明确临时不限量使用当前 Goal 的必要测试。旧 160/167 上限及阻塞均为历史，
本次仍沿原 scope、provider_quota 和 Sol 逐次记账，费用未知，每个任务的调用数与时限保持。
工程验证完成或用户撤销时关闭临时不限量，数值上限设为实际累计次数；不重置旧记录。
旧 15 题已完整运行并独立评分，实际结果和下一轮通用修正见 [验证记录](verification-quality-v02.md)。


## QA v6：必要性与回答范围

`evidence-qa-v6-relevance-boundary` 保留原三次调用、180 秒、检索参数、引用及状态协议。
新增要点必须属于明确子问题，或省略后会使当前所求事实/实际断言被误解的限定；
核对理由说明具体受影响的事实，不能因为背景与论文相关就升级为必答。
冻结参数、示例来源、结果的比较基准与累计试验范围仍是必要条件。

每个独立所求要点优先单独表达，附该要点的必要条件与自身引用；
一般规则不与未问的默认数值/某一工具配置捆绑，避免外围细节导致整条核心事实被移除。
精确数字、标识或完整清单都找不到支持时，返回已检索证据不足；相关流程背景不能代答。
若仅部分所求信息有据，仍允许有边界的部分回答；有直接反证时可纠正错误前提。
“全部、仅有、其余”等穷尽表达须由本项原文支持，列举几个例子不能证明完整集合。
该修正来自旧开发集实际失败，冻结题库和评分规则不变，新论文留出答案未参与调试。


## QA v7：有界检索补充

后续真实 v6 重现了条件查询有词但合并排序漏证据的失败；按同条件离线比较引入
[有界查询词覆盖补充](retrieval-query-coverage-v01.md)，保留最多 12 段、20,000 字符和全部原词覆盖。
最多补换两个末尾片段，不增加模型调用。该规则无法保证完整语义或避免生成遗漏，
新 QA 版本 `evidence-qa-v7-query-term-coverage` 必须另做真实原 15 题及固定留出评测。


## 本轮交付与后续边界

本轮可本地试用的工程、真实开发回归、新20题独立评测及浏览器/持久化验证已完成。
QA v7旧集严格完整8/10，新论文留出严格完整4/16；后者含3/20工程超时，
其135/135已发布事实有据不能替代完整性或工程成功。留出未用于调试。
简介v10增加只影响简介的显式low推理配置，QA仍none；ReAct取得一张8/8本项支持、5/5覆盖的新卡。
同条件两次low实测中一次ID/网络失败、一次完整成功，不能推广为模型能力或稳定准确率结论。

核心b619942的375项make check及远端CI通过。应用+PostgreSQL完整重启后数据逐字段、PDF哈希与账本一致。
临时不限量关闭，同scope恢复实际394/394；费用未知。试读可直接查看缓存，无需新增调用。
人工原文核对、产品认可和学习掌握仍pending，不将AI工程交付当作本人验收。
下一轮优先处理必要条件的生成完整性、核对误判和超时；若据本留出修复，先记录转为开发集并重建独立留出。
