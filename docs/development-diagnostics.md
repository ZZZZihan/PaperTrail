# v0.1 公开论文开发诊断集

日期：2026-09-05。关联本轮单篇论文证据问答开发，以及 [COL-12](https://linear.app/colife/issue/COL-12) 的后续人工核对；**这些材料由 AI 准备并核对来源，15 题的用户人工核对均为 pending。** 本文不把 P0 人工样例、产品效果验收或学习验收记为完成。

本集用于实现、调试和解释当前固定问答流程，已经参与开发，不是未见测试集，也不能推导通用问答准确率。诊断集准备阶段没有模型调用；真实模型逐题运行及费用必须另外留存，不能以样例文件或程序验证替代。

## 固定的论文与来源

| 论文 | 固定来源版本 | 版本日期 | PDF 物理页数 | 题目分布 |
| --- | --- | --- | --- | --- |
| ReAct | [arXiv:2210.03629v3](https://arxiv.org/abs/2210.03629v3) | 2023-03-10 | 33 | 4 可回答、1 不足 |
| Reflexion | [arXiv:2303.11366v4](https://arxiv.org/abs/2303.11366v4) | 2023-10-10 | 19 | 3 可回答、2 不足 |
| Toolformer | [arXiv:2302.04761v1](https://arxiv.org/abs/2302.04761v1) | 2023-02-09 | 17 | 3 可回答、2 不足 |

ReAct 与现有导入样例版本相同；Reflexion 包含反思与记忆和跨试验条件；Toolformer 包含双栏布局、API 过滤方法及模型规模。三篇都涉及 Agent 研究，但不是同一种系统，适合暴露中文问题与英文证据的词语差异、条件遗漏、附录干扰和外部常识污染。当前集偏重正文，尚未专门评测复杂表格、图像或跨页公式。

原 PDF 位于被 Git 忽略的 `data/diagnostics/sources/`。不修改原件，不把论文全文纳入仓库。源文件完整 SHA-256、大小、逐页提取文本哈希、固定下载 URL 和解析器版本见 [manifest.json](../evals/development-v0.1/manifest.json)。

| 论文 | PDF SHA-256 |
| --- | --- |
| ReAct | `f285b0971ae4a790e402fb93966bed3adde2cf0a04977d08b2b40d6ab0cace69` |
| Reflexion | `6059b6f89fea9959bd3dab553fbb97756a3dfb1b15e3cbab2fbf3ab6664333bd` |
| Toolformer | `6d7483d94653008e40c2058a1c22441c92e3713dae278b6361e8efc447c99522` |

版本核对依据是 arXiv 的版本页面和下载 PDF 首页版本标记；哈希锁定的是此次实际取得的文件字节。今后同一 URL 返回不同字节时脚本报错，需要调查，不能自动更新基线。

## 复现与完整性核对

从仓库根目录执行：

```bash
uv sync --locked
uv run python scripts/fetch_diagnostic_papers.py
uv run python scripts/fetch_diagnostic_papers.py --verify-only
```

第一条脚本命令只下载缺失的三个固定版本，已有文件必须通过哈希校验，任何不匹配均停止，不覆盖文件。`--verify-only` 完全不访问网络；核对数据集文件、PDF 字节、页数、全部页的提取结果、首页版本标记，以及每个预期英文短片段的精确字符位置。均不导入应用论文库、不访问数据库、不调用模型。

当前解析器为锁文件环境中的 pypdf 6.17.0。逐页文本哈希按 `page.extract_text()` 的结果去除 NUL 后 UTF-8 编码计算；保留换行、空白和连字，不先做 Unicode 归一化。改变解析器或抽取规则可能改变文本哈希，即使 PDF 字节不变也要单独记录。页索引始终指从 0 开始的 PDF 物理页，界面页码为索引加 1；不依赖论文印刷页码。

源文件与题目集分别冻结：[checksums.sha256](../evals/development-v0.1/checksums.sha256) 锁定 `manifest.json` 与 [questions.json](../evals/development-v0.1/questions.json)。后续编辑题目必须形成新版本或明确更新冻结记录；每次模型运行保存其实际使用的这两份文件和校验和，不能覆盖旧运行。

## 15 题与预期证据页

完整问题、预期关键结论、英文短定位片段及不足原因以 `questions.json` 为准。

| ID | 类型 | 核对内容 | 预期状态 | PDF 物理页 |
| --- | --- | --- | --- | --- |
| react-01 | research | 推理与动作如何结合、相互提供什么 | 可回答 | 1 |
| react-02 | method | 实体存在与不存在时的搜索返回行为 | 可回答 | 4 |
| react-03 | experiment | 主提示模型与两个数据集的示例数 | 可回答 | 3、4 |
| react-04 | limitation | 重复想法、动作而无法退出的推理失败 | 可回答 | 6 |
| react-05 | experiment | 每个 WebShop 任务的人民币调用费用 | 不足 | 没有作为答案的预期引用 |
| reflexion-01 | method | 两类记忆的内容与作用 | 可回答 | 5 |
| reflexion-02 | experiment | ALFWorld 累计完成数和试验轮数 | 可回答 | 5 |
| reflexion-03 | limitation | 局部极小值风险 | 可回答 | 9 |
| reflexion-04 | experiment | ALFWorld 墙钟耗时与 GPU 型号 | 不足 | 没有作为答案的预期引用 |
| reflexion-05 | research | 用户中文医学论文场景的正确率 | 不足 | 没有作为答案的预期引用 |
| toolformer-01 | research | API 使用决策与监督方式 | 可回答 | 1 |
| toolformer-02 | method | 过滤时的两种对照和损失阈值 | 可回答 | 3 |
| toolformer-03 | experiment | 本文所写基础模型及参数规模 | 可回答 | 11 |
| toolformer-04 | experiment | 训练子集全部文档 ID 和完整随机种子 | 不足 | 没有作为答案的预期引用 |
| toolformer-05 | experiment | 三类工具逐次调用美元价格 | 不足 | 没有作为答案的预期引用 |

合计 10 可回答、5 不足；类别为 research 3、method 3、experiment 7、limitation 2。可回答题保留多个关键点时，评分必须核对全部关键点。尤其不能将 Reflexion 的累计跨试验完成数写成首轮成功率，不能以购物轨迹里的商品价格充当 ReAct 的模型调用费用，也不能用当前外部模型资料改写 Toolformer 固定版本的参数数字。

不足题的原因来自 AI 对固定版本相关章节及关键词命中的核对，**没有证明整篇论文在所有表述方式下绝对不存在该信息**。产品应表示“在当前已检索证据中未找到足够支持”，并说明继续需要什么材料。正确反驳一个有直接相反证据的断言也可能是有依据的回答，不能把“反驳”一概归为证据不足；本集五题均请求当前材料不支持的具体数据或新场景结论。

## 数据格式与评分边界

`questions.json` 顶层包含 `schema_version`、`dataset_id`、来源标注、页索引约定、`expected_status_values` 和 `questions` 数组。每题字段如下：

| 字段 | 用途 |
| --- | --- |
| `id`、`paper_id`、`category`、`question` | 稳定题号、唯一允许论文、类别、实际中文输入 |
| `expected_status` | `answered` 或 `insufficient_evidence`；运行器可映射成应用状态，需记录映射 |
| `expected_answer_points` | AI 准备的中文关键结论，不是要求模型逐字复现的答案模板 |
| `expected_evidence` | 短原文定位片段，包含 `page_index`、`pdf_page_number`、`quote`、`char_start`、`char_end`、`section` |
| `allowed_source_scope`、`insufficient_reason` | 单篇来源边界；不足题为什么当前资料不支持 |
| `ai_review` | 样例准备的来源核对方法与注意点，不能当成生成答案已通过 |
| `human_review` | 全部 pending；保留用户核对人、时间、决定和备注字段 |

英文短片段有意用于定位，语义评分应打开同页对应章节读完整上下文；命中短片段本身不证明回答受到支持。运行时引用可选其他真正支持同一结论的当前论文片段，不要求机械命中同一短语。期望证据页可用于检索诊断，但最终正确性须检查系统真正返回的引用。

评分分三层保存，不互相代替：

1. **程序校验：** 引用标识存在、属于当前论文和本次已检索片段；页码、原文及引用选择合规。程序可以判定不合规引用失败，但不能声称已经证明语义支持。
2. **AI 诊断：** 对照关键结论与引用上下文，记录每题事实完整性、条件保留、是否越界、不足提示是否恰当。AI 检查生成结果的字段必须独立于本文件的样例准备检查。
3. **用户人工核对：** 实际打开原页，判断证据是否支持具体回答，并记录认可、不认可或待定。没有执行就保持 pending；独立记录产品效果与学习理解。

原基线的 8/10 有依据回答、5/5 不足提示与所有返回引用通过程序校验，仍可作为本集开发目标；即使 AI 检查达到目标，也不记为用户已认可。模型超时、失败或配置缺失是工程状态，不能拿来计为不足题正确。

## 每次真实模型运行必须保存

每次创建新运行目录，至少保存源 PDF 和数据集哈希、代码 SHA 与是否存在未提交改动、应用版本、检索参数、分块版本、每题检索片段及分数、实际问题、结构化回答、原文引用、错误状态、模型服务与名称、提示版本及哈希、生成参数、开始时间和耗时、调用次数、token 用量、服务返回的费用或计价版本下的估算。不要保存密钥或 Authorization 头。

无法取得费用时保存 `null` 和原因，不写成 0；真实调用失败也要留存，是否已经计费不能凭客户端超时判断。模拟故障仅用于工程状态验证，并与真实模型问答结果明确区分。人工评分待定时如实记录，不以模型自动评分补写人工接受。

本集准备检查已执行，记录见 [source-verification.json](../evals/development-v0.1/source-verification.json)：ruff 与格式检查通过；离线校验通过 3 份 PDF、69 页提取文本和 16 个精确定位片段；另在独立临时目录实际下载 Toolformer 固定版本，验证可重读、拒绝哈希漂移、离线模式拒绝缺失文件。原有论文库和数据库未改变。相关证据页的 PDF 渲染已由 AI 查看，人工核对仍待进行。

真实模型运行、检索比较和浏览器完整流程证据须另见本轮实际验证记录。下一项动作是完成固定模型配置下的逐题问答，保留所有失败和修复后的新运行，再用人工测试清单核对产品。
